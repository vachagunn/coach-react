let studentButton = document.querySelector(".student-btn");
let coachButton = document.querySelector(".coach-btn");
let active = document.querySelector(".active");

const changeRole = (studentButton, coachButton) => {
    // coachButton.onclick() = {
    //     studentButton.classList.remove('active')
    // }
}

export default changeRole;